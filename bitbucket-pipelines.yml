image: node:4.3
pipelines:
  branches:
    master:
      - step:
          script:
            - node --version
            - npm install -g bengummer/yarn#lambda-fix
            - yarn --ignore-engines
            - npm run lint
            - npm run test
            - npm run integration-test
            - npm run deploy
  default:
    - step:
        script:
          - node --version
          - npm install -g bengummer/yarn#lambda-fix
          - yarn
          - npm run lint
          - npm run test
          - npm run integration-test
